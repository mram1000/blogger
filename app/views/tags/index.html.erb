<h1> All Tags </h1>
<ul id="articles">
  <% @tags.each do|tag| %>
  <li><%= link_to tag.name, tag_path(tag), class: "tag_name" %> <%=link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "Really delete the tag?"} %><br></li>
  
  <% end %>
</ul>
